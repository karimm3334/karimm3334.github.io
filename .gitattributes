# Keep text files as LF in the repo for consistent cross-platform diffs.
* text=auto eol=lf

# Treat these as binary (no line-ending conversion).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.mp4 binary
*.webm binary
*.ico binary
